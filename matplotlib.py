import matplotlib
import matplotlib.pyplot as plt
matplotlib.use('TKRgg')
def selection_sort(seq):
    n=len(seq)
    for i in range(n):
        min=i
        for j in range(i+1,n):
            if seq[i]<seq[min]:
                temp=seq[min]
                seq[min]=seq[j]
                seq[j]=temp
seq=[10,70,15,8,80,90,20]

print("Before Sorting",seq)
selection_sort(seq)
print("after sorting",seq)
X=list(range(1.10000))
plt.plot(z,[Yy for y in x])
plt.xlabel("Selection_Sort_Time_complexty is o(n)400b2)")
plt.xlabel("input")
plt.ylabel("Time")
plt.show()

      




            
            
            
